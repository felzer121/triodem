import { createTheme } from '@mui/material/styles';
import { green, purple } from '@mui/material/colors';

export const theme =  ({
  palette: {
    primary: {
      main: '#FFD400',
    },
    secondary: {
      main: green[500],
    },
  },
});